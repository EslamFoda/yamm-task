# Yamm Task

This task shows a basic dashboard table for refund orders with pagination.

## Development

To run the development environment, follow these steps:

- Clone this repo.
- Run these commands in the terminal:

  ```sh
  npm run install
  npm run serve

- Run this in a different terminal:
  
  ```sh
  npm run dev